<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ExpenseTracker.Mobile.View.AddExpensePage">
    <ContentPage.Content>
        <StackLayout>
            <Entry Placeholder="Description" Keyboard="Text" Text="{Binding Description}"></Entry>
            <Entry Placeholder="Amount" Keyboard="Numeric" Text="{Binding Amount, Mode=TwoWay}"></Entry>
            <Picker ItemsSource="{Binding CategoriesList.Items}" 
                    ItemDisplayBinding="{Binding Name}" 
                    Title="Select Category">
            </Picker>
            <Button Text="Save"
                    Command="{Binding SaveButton.Command}"
                    IsEnabled="{Binding SaveButton.IsEnabled}"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>